

  <script type="text/javascript">
    var geometries = [];
    //var layers = [];
    var geometry_labels = [];
    var exist_features = [];
    var exist_geometries = [];
    var exist_labels = [];
    var map, drawControls, walking_paths_survey, walking_paths_survey_labels;
    function init(){
      var layers = [ 
      new ol.layer.Image({
        extent: [-122.5,47.5,-122.2,47.75],
        source: new ol.source.ImageWMS( ({
          url: 'http://localhost/cgi-bin/mapserv?map=/home/paul/mapserver/gs_team_study_areas.map',
          params: {'LAYERS': 'MS', 'VERSION': '1.1.1', 'SRS':'EPSG:4326' },
          serverType: 'mapserver'
      }))
    })
  ];


  var map = new ol.Map({
    layers: layers,
    target: 'map',
    view: new ol.View({
      projection: 'EPSG:4326',
      center: [-122.4,47.6],
      zoom: 10
    })
  });

  }

  </script>
